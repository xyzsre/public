apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-helm-app.fullname" . }}
  labels:
    {{- include "simple-helm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-helm-app.selectorLabels" . | nindent 4 }}
